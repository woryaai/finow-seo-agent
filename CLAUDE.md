# Claude Code entry point

This project uses AGENTS.md as the shared operating contract. Native project commands live under .claude/commands/ and canonical command specifications live under commands/.
