---
description: Build a new Finow site/microsite foundation when appropriate
argument-hint: "[arguments optional]"
---

Read `AGENTS.md`, then execute the canonical command specification in `commands/finow-build-website.md`.

Arguments from the invocation:

`$ARGUMENTS`

Treat those arguments exactly as command arguments. Do not duplicate or override the canonical command logic in this wrapper.
